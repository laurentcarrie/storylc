input boxes ;
input TEX ;
%input latexmp ;
%input storylc/lib/slide


vardef scene(expr i,duration,ips)=
		%mygrid ;
		%transform t ;
		%t = identity shifted (0cm,3cm) scaled 1 ;
		%gitremote(t,"blah",0) ;
		draw origin -- (10,10) ;
enddef ;

