\documentclass[11pt]{article}
\usepackage{animate}
\usepackage{graphicx}
\usepackage{media9}
\usepackage{animate}
\usepackage{graphicx}
\usepackage{media9}
\usepackage{luamplib}


\begin{document}
\mplibtextextlabel{enable}

\section{ {{ animation.name }} }
    \begin{center}
        \animategraphics[
            label={{ animation.name }},
            controls, loop,
            timeline=timeline.txt
        ]{ {{animation.ips}} }{mps/frame_}{ {{ 0 }} }{ {{ animation.duration * animation.ips -1}} }
        \mediabutton[
        jsaction={
            if(anim[’{{animation.name}}’].isPlaying)
            anim[’{{animation.name}}’].pause();
            else
            anim[’{{animation.name}}’].playFwd();
        }
        ]{\fbox{Play/Pause}}
    \end{center}


\end{document}
